extends layout

block content
  h1= title
  script(type='text/javascript').
      google.charts.load('current', {'packages': ['gauge']});
      google.charts.setOnLoadCallback(drawChart);
      function drawChart() {


          var cData = google.visualization.arrayToDataTable([
              ['Label', 'Value'],
              ['#{data[0][0]}', #{data[0][1]}],
              ['#{data[1][0]}', #{data[1][1]}],
              ['#{data[2][0]}', #{data[2][1]}],
              ['#{data[3][0]}', #{data[3][1]}],
              ['#{data[4][0]}', #{data[4][1]}],
              ['#{data[5][0]}', #{data[5][1]}]
          ]);
          var options = {
              min:0,
              max: 16,
              width: 1000, height: 320,
              redFrom: 12, redTo: 16,
              yellowFrom: 10, yellowTo: 12,
              minorTicks: 2
          };
          var chart = new google.visualization.Gauge(document.getElementById('chart_div'));
          chart.draw(cData, options);
          // setInterval(function () {
          //     data.setValue(0, 1, 40 + Math.round(60 * Math.random()));
          //     chart.draw(cData, options);
          // }, 13000);
          // setInterval(function () {
          //     cData.setValue(1, 1, 40 + Math.round(60 * Math.random()));
          //     chart.draw(cData, options);
          // }, 5000);
          // setInterval(function () {
          //     cData.setValue(2, 1, 60 + Math.round(20 * Math.random()));
          //     chart.draw(cData, options);
          // }, 26000);
      }
  #chart_div